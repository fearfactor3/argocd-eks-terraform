# OpenTofu / Terraform
.terraform/
*.tfstate
*.tfstate.*
*.tfstate.backup
crash.log
crash.*.log
.terraform.tfstate.lock.info
.terraformrc
terraform.rc

# Variable files — per-environment tfvars are tracked (no secrets);
# only ignore auto-loaded and local override files.
terraform.tfvars
terraform.tfvars.json
*.auto.tfvars
*.auto.tfvars.json
*.local.tfvars

# Plan files
*.tfplan
tfplan

# Override files (local environment overrides)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# MegaLinter
megalinter-reports/

# Spacelift
.spacelift/

# OS
.DS_Store
Thumbs.db

# Editor
.idea/
.vscode/
*.swp
*.swo
